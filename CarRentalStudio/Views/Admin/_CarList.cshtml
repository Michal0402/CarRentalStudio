@model IEnumerable<CarRentalStudio.Models.Car>
<div style="margin-bottom: 10px;">
    <a href="#" id="add-car-link" class="add-car-link">Dodaj</a>
</div>
<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Marka</th>
            <th>Model</th>
            <th>Rok produkcji</th>
            <th>Zarządzaj</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var car in Model)
        {
            <tr>
                <td>@car.Id</td>
                <td>@car.Brand</td>
                <td>@car.Model</td>
                <td>@car.Year</td>
                <td>
                    <a href="@Url.Action("EditCar", "Admin", new { id = car.Id })" class="edit-car-link btn btn-warning">Edytuj</a>
                    <a href="@Url.Action("DeleteCar", "Admin", new { id = car.Id })" class="delete-car-link btn btn-danger">Usuń</a>
                </td>
            </tr>
        }
    </tbody>
</table>

